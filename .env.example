# Copy to .env (gitignored). Values arrive from Bader OUT-OF-BAND — never
# commit real values, never paste them into chat or issues.

TEAM_SEAT=              # your seat slug: bader / sjp / amr / adham (manager = bader)
TEAM_HEARTBEAT_SECRET=  # console auth (needed from D3)

# Project runtime keys are appended here at the Day-0 stamp (LLM API, etc.)
