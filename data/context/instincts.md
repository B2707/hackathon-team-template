# Instincts — when X, do Y, because Z

<!-- CAP ~30 lines; pruned at every promotion. Only the nightly distill PR
     writes here (human-approved). Candidates live in instinct-candidates/
     and are NEVER loaded directly.

     STATUS: DORMANT this event, by cut (design A16) — no in-event producer is
     wired; the gotchas reflex layer (/gotcha -> candidates -> FM promotion)
     carries in-event learning. Do not re-litigate or build more mid-event. -->
