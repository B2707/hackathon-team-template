# Decisions

One file per decision: `NNN-slug.md` — what was decided, why, and what was
rejected. Created via PR (usually by the FM or Bader). Agents cite these
files instead of re-litigating settled questions. `/ship` may append a new
decision file when the brief calls for one.
